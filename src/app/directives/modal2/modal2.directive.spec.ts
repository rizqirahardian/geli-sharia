import { Modal2Directive } from './modal2.directive';

describe('Modal2Directive', () => {
  it('should create an instance', () => {
    const directive = new Modal2Directive();
    expect(directive).toBeTruthy();
  });
});
