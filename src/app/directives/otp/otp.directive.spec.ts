import { OtpDirective } from './otp.directive';

describe('OtpDirective', () => {
  it('should create an instance', () => {
    const directive = new OtpDirective();
    expect(directive).toBeTruthy();
  });
});
